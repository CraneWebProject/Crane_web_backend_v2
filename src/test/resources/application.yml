spring:
  datasource:
    driver-class-name: org.mariadb.jdbc.Driver
    url: jdbc:mariadb://192.168.0.4/crane_web_backend_test
    username: root
    password: Phasam2023IoT
  jpa:
    hibernate:
      ddl-auto: update
      naming:
        physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
    defer-datasource-initialization: true
    show-sql: true  # SQL 쿼리 로그 출력
    properties:
      hibernate:
        format_sql: true  # 포맷된 SQL 출력
  sql:
    init:
      mode: always
    script: classpath:data.sql

  jwt:
    header: Authorization
    secret: U29vbkNodW5IeWFuZ1VuaXZlcnNpdHlDZW50cmFsQ2x1YlJvY2tCYW5kQ1JBTkVXZWJCYWNrZW5kUHJvamVjdEpXVFNlY3JldAo=
    access-expiration-time: 86400000 #60분
    refresh-expiration-time: 604800000 #1주(7일)
# JPA 관련 추가 설정
logging:
  level:
    org.hibernate.SQL: DEBUG  # Hibernate SQL 로그
    org.hibernate.type.descriptor.sql.BasicTypeRegistry: TRACE  # Hibernate Type Log

server:
  port: 8080
  ssl:
    key-store: keystore.p12
    key-password: schcrane2024
    key-store-password: schcrane2024
    key-store-type: PKCS12
    key-alias: tomcat
    enabled: true